/- STAGE FAMILY A: "first generator per target" (Fu0, Fu2, Fv0, Fv2).

Shape: peel variable `x` (one of U0,U1,V0,V1), generator `g = c - X x * d`
where `c, d : Rdec p` come entirely from ONE side's data (so their `vars`
avoid `x` by `u1_indep`/`v1_indep`, since `x ∈ {U0,U1,V0,V1}` and those
Finsets only ever contain `{wa1,wa2,a1,a2}`/`{wb1,wb2,b1,b2}`). The
"already-imposed prefix" at this stage consists ONLY of earlier `Fu`/`Fv`
generators, each ALSO avoiding `x` (by the same variable-independence
facts, since `x` hasn't been introduced by any earlier generator either --
confirmed by the peel-order check above).

This needs:
  (a) `d ≠ 0` (from `denRegular`) lifted to `d ≠ 0` in `Rdec p` (a domain,
      MvPolynomial over a field), hence `IsSMulRegular (Rdec p) d` (regular
      in the AMBIENT ring, self-action) via `mul_right_cancel₀`-style
      domain reasoning -- NOT yet "regular mod prefix", that's a further
      step.
  (b) `d`'s regularity survives quotienting by the prefix. Since prefix's
      variables and `d`'s variables and `x` are three DIFFERENT things to
      track: `d` itself doesn't mention `x` (side-independence), and the
      PREFIX doesn't mention `x` either -- but this alone is NOT what
      `regular_of_disjoint_extension` needs. That lemma's actual shape:
      given `e : MvPolynomial σ₂ R` regular in ITS OWN ring, and `g : MvPolynomial σ₁ R`
      arbitrary, `e`'s image survives quotienting by `⟨g⟩` PROVIDED `e`
      lives in a variable set (`σ₂`) DISJOINT from `g`'s (`σ₁`). Here we'd
      want `σ₁ := "prefix's variables"`, `σ₂ := "d's variables"` -- but the
      prefix is a LIST of generators, and `regular_of_disjoint_extension`
      only handles ONE generator `g` at a time (`Ideal.ofList [g]`, a
      SINGLETON). So this needs an ITERATED application, once per prefix
      generator, chaining through -- OR, more directly, an inductive
      argument that `IsSMulRegular (Rdec p) d` (ambient-ring regularity,
      i.e. `d ≠ 0` since `Rdec p` is a domain) survives quotienting by an
      ARBITRARY LIST of generators none of which mention `d`'s variables.
      THIS iterated/list version is not literally `regular_of_disjoint_extension`
      (stated for one generator) -- needs its own small wrapper, OR
      restate the disjointness argument for `Ideal.ofList gens'` directly
      (`σ₁ := ⋃ variables of every prefix generator`) rather than
      one-at-a-time. The one-shot route is cleaner: generalize
      `regular_of_disjoint_extension`'s STATEMENT from a single `g` to a
      LIST `gens' : List (MvPolynomial σ₁ R)`.

DECISION: add `regular_of_disjoint_extension_list`, the direct
list-generalization of the already-proved single-generator version, via
`Ideal.ofList gens' = Ideal.ofList [g]` is FALSE in general for a list of
length > 1 -- but the SAME base-change argument (flatness of
`MvPolynomial σ₁ R ⧸ Ideal.ofList gens'` over `R`, still true since
EVERY quotient of a free/flat module by a submodule generated by
elements from the "σ₁-only" part remains flat after tensoring up -- this
is NOT generally true for arbitrary quotients, flatness does NOT descend
to quotients in general! The single-generator proof crucially used that
`MvPolynomial σ₁ R ⧸ ⟨g⟩` is a specific ring, appealed to via
`Module.Flat.of_field`+`baseChange`, i.e. flatness of THAT PARTICULAR
quotient over the FIELD R (every module over a field is flat) -- this
argument is TOTALLY INSENSITIVE to how many generators `⟨g⟩` needs, i.e.
it works verbatim for `Ideal.ofList gens'` too, since `[Field R]` was the
ONLY input to flatness, not anything about `g` specifically!

So `regular_of_disjoint_extension`'s PROOF generalizes to a list
essentially unchanged (replace `Ideal.span {g}`/`Ideal.ofList [g]`
throughout with `Ideal.ofList gens'`, and `hE₁_inl`'s single-generator
identity with a `List.map`-level one) -- this is a real, but mechanical,
generalization, not a new mathematical fact. Given the proof is ~140
lines already, RE-DERIVING it for a list is a lot of surface area to
duplicate by hand without a REPL. -/
